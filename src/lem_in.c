/*
** EPITECH PROJECT, 2018
** Lem-in
** File description:
** lem_in.c
*/

#include <stdio.h>
#include <stdlib.h>

int parsing(void)
{
	struct anthil anthil = malloc(sizeof(anthil));
	char *cur_data = NULL;

	if (!anthil)
		return (84);
	return (0);
}

int main(void)
{
	parsing();
	return (0);
}
